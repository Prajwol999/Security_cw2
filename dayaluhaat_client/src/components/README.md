# Components

This directory contains reusable UI components for the application.

## Structure
- Each component should have its own folder if it has multiple related files (styles, tests).
- Shared/Generic components go here.
- Page-specific components can go in `pages/<PageName>/components`.
